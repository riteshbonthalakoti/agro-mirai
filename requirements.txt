# AGRO MIRAI — runtime dependencies (API + web frontend).
#
# Generated from `pip freeze` against the global interpreter that has run
# every module's tests, hand-checked against actual `import` statements
# across src/, tools/, and tests/ (excluding tests/voice/) via:
#   grep -rhoE '^\s*(import|from)\s+[a-zA-Z0-9_\.]+' src tools tests
#
# Deliberately EXCLUDED: the Module 12 voice/AI4Bharat stack (torch,
# transformers, indicnlp, sacremoses, regex, soundfile, piper, torchaudio).
# See decisions/0014-deploy-target-and-voice-scope.md — the hosted Render
# instance does not run live voice inference; it is demoed locally from
# the project-local .venv/ instead. requirements-dev.txt covers test-only
# tooling.
#
# Pinned to the exact versions this codebase has been tested against.
# Crop and irrigation advice are rule-based (decisions/0027, 0029), so
# there are no scikit-learn/SHAP model files to keep in sync any more.

Flask==3.1.3
Werkzeug==3.1.8
Jinja2==3.1.6
MarkupSafe==3.0.3
itsdangerous==2.2.0
click==8.4.2
blinker==1.9.0

numpy==2.5.2

PyYAML==6.0.3
python-dotenv==1.2.2
requests==2.34.2

# Module 04 — SupabaseDataStore (prod persistence)
supabase==2.31.0
postgrest==2.31.0
supabase-auth==2.31.0
supabase-functions==2.31.0
storage3==2.31.0
realtime==2.31.0
httpx==0.28.1

# Module 15 — production WSGI server.
# gunicorn does not run on Windows (no fcntl); Render's build/runtime is
# Linux, so this is fine for deploy. Local Windows dev keeps using
# `flask run`, and the multi-worker verification for this module was run
# under WSL (see decisions/0014-deploy-target-and-voice-scope.md).
gunicorn==26.2.0

# Module 16 — reliability & CI hardening.
# Per-API-key rate limiting; in-memory store (single-process deploy only —
# a multi-worker deploy would need a shared store like Redis).
Flask-Limiter==4.1.1
limits==5.8.0
ordered-set==4.1.0
Deprecated==1.3.1
# Error monitoring. No-op with no SENTRY_DSN configured — see app.py.
sentry-sdk==2.68.1

# Module 21 — image-upload content sniffing for POST
# /fields/{field_id}/disease-risk/image (real decode/verify, not just
# trusting the filename extension or Content-Type header).
Pillow==11.0.0

# Module 19 — real login/session auth (multi-tenant v2).
# bcrypt for password hashing (established KDF library, not hand-rolled) —
# see decisions/0017-multi-tenant-v2.md.
bcrypt==5.0.0

# Module 24 (compatibility hardening) — CORS headers so browser clients
# (Next.js landing page, React Native web build) can reach the /v2 API.
# Configured with CORS_ORIGINS env var; unset means same-origin only.
Flask-Cors==6.0.5

# Module 40: satellite NDVI (Earth Engine) was silently unavailable on Render
# because this library was never installed there.
earthengine-api==1.7.41
