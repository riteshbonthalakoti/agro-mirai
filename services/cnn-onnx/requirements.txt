# Deliberately tiny: ONNX Runtime instead of torch so this fits Render's free
# 512 MB tier (torch alone needs 400+ MB). See decisions/0025-onnx-cnn-service.md.
onnxruntime==1.20.1
numpy==2.1.3
Pillow==11.0.0
Flask==3.1.3
Werkzeug==3.1.8
gunicorn==26.2.0
