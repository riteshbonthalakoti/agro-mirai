Two things need fixing before we go further — I checked the actual code, not just the handoff summary.

1. **Remove Telugu and Hindi entirely.** The backend's `V1_LANGUAGES` and every validation on `PATCH /v2/farmers/me` only accept `{en, kn}` — `te`/`hi` will 400 the moment this hits the real API, and nobody asked for four languages; the spec and every design doc say two (Kannada, English). Revert the language picker, the `language` state type, `getTtsLangCode`, and every downstream use back to `'en' | 'kn'` only. Don't leave the other two "for later" — remove them now so there's no dead/misleading code path.

2. **Wire the real auth flow — the current one is mocked.** `App.tsx` has a `// Mock seed / login demo handling` comment where the register/login logic should be. Replace it with real calls: `POST /v2/auth/register` (email, password, name, preferred_language), then an explicit `POST /v2/auth/login` (register does not auto-session — this must be two calls, not one), storing the resulting session cookie via `expo-secure-store` and attaching it on every subsequent request. Test this against the real local backend (`http://10.0.2.2:5000` from the Android emulator, or your actual dev-machine LAN IP if testing on a physical device) — register a real farmer, log in, then confirm `GET /v2/fields` returns real data for that session before calling this done.

Once both are fixed, re-verify the full loop for real: register → login → create/select a field → Home screen hero button plays a real advisory's audio from `GET /v2/advisories/{id}/audio`, or falls back to on-device TTS on a real `503`. Report back with what you actually tested each step against (which endpoint, which response), not just "it works."
